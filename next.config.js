/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

module.exports = {
  images: {
    formats: ["images/avif", "image/webp"],
    remotePatterns: [],
    domains: ["lh3.googleusercontent.com", "randomuser.me"],
  },
};
